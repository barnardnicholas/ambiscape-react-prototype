import backgroundData from "./background/background-index";
import randomData from "./random/random-index";

const audioData = {
  background: backgroundData,
  random: randomData
};

export default audioData;
